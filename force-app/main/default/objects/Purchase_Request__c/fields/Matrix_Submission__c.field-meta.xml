<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Matrix_Submission__c</fullName>
    <defaultValue>false</defaultValue>
    <description>The framework guard (arch doc 3.4, 6.3). Set true by the engine only, in the same transaction as its own submission, and reset to false by every template&apos;s final approval and rejection actions. Every Classic template requires it true in entry criteria, so a submission that bypasses the engine fails loudly instead of routing silently. A framework convention, never a configurable name.</description>
    <externalId>false</externalId>
    <label>Matrix Submission</label>
    <trackTrending>false</trackTrending>
    <type>Checkbox</type>
</CustomField>
